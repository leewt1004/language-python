## 18.5 연습문제
"""
3으로 끝나는 숫자만 출력하기
다음 소스 코드를 완성하여 0과 73사이의 숫자 중 3으로 끝나는 숫자만 출력되게 만드세요.

i = 0
while True:
    (1)____________         
        ___________
        ___________
    (2)____________
        ___________
    print(i, end=' ')
    i += 1

- 실행 결과 - 
3 13 23 33 43 53 63 73
"""

i = 0
while True :
    if i % 10 != 3 :
        i += 1
        continue
    if i > 73 :
        break

"""
i = 0                   -> 변수 i를 0으로 초기화합니다. 이 변수는 반복문 안에서 증가하며 조건에 따라 흐름을 조절하는 기준이 됩니다.
while True :            -> True를 조건으로 하여 무한 루프를 시작합니다. 이 루프는 break 문이 실행되기 전까지 계속 반복됩니다.
    if i % 10 != 3:     -> i를 10으로 나눈 나머지가 3이 아닌지 확인합니다. 즉, i의 일의 자리 숫자가 3이 아닐 때 조건이 참이 됩니다.
        i += 1          -> 조건이 참일 때 i를 1씩 증가시킵니다. 이로 인해 i가 점차 커지게 됩니다.
        continue        -> continue 문은 현재 반복의 나머지 부분(아래의 코드)을 건너뛰고 다음 반복으로 이동하게 합니다. 즉, i % 10 != 3 조건이 참이면 i를 증가시키고 나머지 코드를 건너뜁니다.
    if i > 73 :         -> i가 73보다 큰지 확인하는 조건문입니다. 이 조건이 참이 되면 break 문이 실행됩니다.
        break           -> break 문이 실행되면 while 루프를 종료하고 반복을 끝냅니다. 따라서 i가 73보다 커지는 순간 루프가 종료됩니다.
"""
